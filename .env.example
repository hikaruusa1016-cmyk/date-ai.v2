# OpenAI API Key（オプション：AIによるプラン生成を使う場合）
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Maps API Key（必須：地図表示とPlaces検索に使用）
GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here

# Server Port（開発環境用、Vercelでは不要）
PORT=3001

# Node Environment（本番環境ではVercelが自動設定）
NODE_ENV=development

# Frontend URL（本番環境用、CORS設定に使用）
# 例: https://your-app.vercel.app
FRONTEND_URL=

# Access Token（オプション：本番環境で簡易認証を使う場合）
# ランダムな文字列を生成して設定（例：openssl rand -hex 32）
# 設定しない場合は認証なしで動作します
ACCESS_TOKEN=
